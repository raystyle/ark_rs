# 2026-09-20 ledger 命令族集成 REQ-0015 与封版 v1.5.0

## 总台令

集成 ledger 标准的 issue 与 artifact 命令族替代原 issues.ohmygh.com 面并封小版本（契约权威 ohmycloud REQ-063 Phase 3 CLI 原生集成；服务已上线 ledger.ohmygh.com）。

## 实现 5f48053 加对线修笔

- src/ledger.rs 新模块：Ed25519 五头签名道（签名基七行换行连、幂等键与 nonce 各 uuid v4、私钥读 ARK_LEDGER_KEY 或密档缺省 ~/.config/ark/ledger.key 不进仓不进 argv、公钥 JWK 内置 kid=7f365848…ba18 总台入册）；issue 族（new/list/show/close，close 为 result 引 digest 加 status done 两写链半链态如实报）；artifact 族（publish/attest/promote/list）；家族翻页保持（恒带 more=1 索取 has_more、饱和提示以 has_more 为准）。新依赖 ed25519-dalek/base64/uuid。
- 旧 issue.rs 整撤（issues.ohmygh.com 客户端面退役；http_client/TIMEOUT_MS 迁 ledger 自持），lib.rs 与 aidoc 投影同步。
- 服务端实况对照（workers/ledger/src/index.ts 只读）：kind 十五值（总台单写 16 系笔误）、projection 无 title（show 从 timeline issue_open 取）、issuesList 的 has_more 条件（before 或 more=1）。
- 对线一轮（ark-codex-review）：F1 旧模块第二真相（撤）、F2 首页缺 has_more 与 has_more=false 出截断提示自相矛盾（恒带 more=1 加提示以 has_more 为准）、F3 README 悬空半句、F4 AGENTS 命令面未随批更新、F5 diary 缺钩子（本篇）。
- 踩坑：python 批量脚本 heredoc 内嵌反引号与转义在 bash 侧再解释致替换串失真（三次断言失败后改逐行重写与 Write 工具直书）。

## 实弹 公钥注册后 总台回执在案

issue new 得 issue=1 seq=3（201）；list 投影对；show 出 title/kind/status/timeline；artifact publish（kind=lesson，本仓「批量脚本先落盘后断言」二犯同型真实教训）得 artifact_id 2ffd7f18-5a8c-407c-8ede-696d43d2b2d1 seq=4（201）；attest_dev seq=5；promote seq=6；list --current 得 dev=y current=y。

## 封版 v1.5.0

minor（命令族新增）；release.ps1 -Tag v1.5.0 -IncludePackages。
