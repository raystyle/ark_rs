# 2026-09-16 skill面撤除

## 裁定与立项

用户裁定（2026-09-16）：「有 --llms 我们就不需要 skill 参数命令了，--llms 就是一个紧凑版本的给 agents 的说明书」；追加令「去掉 SKILL 命令和 SKILL 生成，清理五端四机安装的 SKILL」。这是 D09 预留的命令面收敛另批兑现，立 REQ-0005 与 ADR-0007（PRD 冻结声明起 D50 详篇入 ADR、PRD 只指针行），PRD D50 行入表。

两项口径（AskUserQuestion 裁）：

- 撤整面：`ark skill` 子命令、selfdeploy 三函数（deploy_skill 与 write_skill 与 render_skill）、根 SKILL.md 全撤；`--llms` 头部吸收何时用与下载两行纪律，唯一 agent 发现通道，manifest 单源自持。
- guide 四字段保留为数据面：schema 与解析不动（真源云端 ohmycloud，ark 侧无渲染消费者），注释改口径。

## 落码

提交 1f0720d。

- main.rs：Commands::Skill 变体与分派与 cmd_skill 撤；LLMS_MANIFEST 删 skill 行、插何时用与下载两行（原 SKILL.md 价值段四要点全覆盖）；顺修 doctor doc 块错挂在 cmd_skill 头上的旧偏移（撤后自然归位 fn cmd_doctor）。
- selfdeploy.rs：三函数换 remove_legacy_skill（幂等清理 metadata_dir/SKILL.md，仿 ome 别名清理模式，两平台 self_deploy 调用点接 match 清理块）。旧 ohmyenv 位按「只读保留」策略代码不动，归一次性清扫收。
- catalog.rs guide 四字段与 lib.rs crate doc 注释口径同步；根 SKILL.md 删除（与 include_str! 同批）。
- tests：cli.rs llms 断言改何时用加 not contains ark skill；real.rs 缺席名单加 ark skill。

## 冒烟与验证

- cargo fmt 与 clippy 与 test --release --locked 全绿（clippy 13 条 warning 全在 docker.rs，与 HEAD 基线一致非本批引入）；cargo aidoc 再生成 26 件、--check --strict 干净。
- CLI 冒烟：`--llms` 头部两纪律行在、`grep -c "ark skill"` 为 0；`ark skill` 退出码 2（clap unrecognized）；裸 `ark` 缺子命令提示不变；隔离 HOME 加 XDG_DATA_HOME 下 init 首跑 `[OK] 已清理旧 SKILL.md`、二跑静默（幂等实证，未碰真实部署位）。

## 五端四机清扫

双位清扫（`<data>/ark/SKILL.md` 与 `<data>/ohmyenv/SKILL.md`），lan-win 走 127.0.0.1 回环 ssh 加 pwsh EncodedCommand（避引号嵌套坑）：

| 端 | ark 位 | ohmyenv 旧位 |
|---|---|---|
| WSL 本机 | 无 | 无 |
| lan-win | 清 1 | 清 1 |
| lan-mac | 无 | 无 |
| lan-ubuntu | 清 1 | 无 |
| lan-linux | 清 1 | 无 |

lan-win 旧位也有副本，实证双位清扫必要性（D41 迁移七件套不含 SKILL.md）。注意：各端部署二进制仍是 v1.2.3（带 deploy_skill），D50 版铺开前旧二进制 init 会重建 ark 位 SKILL.md，随下一版 self update 自然收敛。

## 治理面

- 批次五提交：REQ 立项（fc4ecb5）、feat 撤面（1f0720d）、docs 单通道口径（0e3aefb）、chore 脚本留档（4f2771f）、治理回填本批。inject-guide-d25.py 不删改留档口径（同 inject-probe-d28 先例：一次性注入脚本留档记内容出处）。
- CHANGELOG Unreleased 开窗；R013 冻结面删 skill 行、R001 四字段转数据面注记；README 与 llms.txt 单通道句。
- 顺带观察（未入本批）：build.yml 残留 `catalog/**` 触发路径（D37 遗留）；LLMS_MANIFEST 头行 47 工具与 AGENTS/README 50 工具计数漂移；diary README 索引补 2026-09-15 批四与 2026-09-16 终态对齐两行欠账（本批顺补）。
- 推送前按 AGENTS 规则还需 herdr 对线 review（codex），回执待记；未经指示不推远端。
