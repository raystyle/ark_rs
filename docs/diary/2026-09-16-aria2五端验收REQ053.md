# 2026-09-16 aria2五端验收REQ053

## 派单

ohmycloud 总台派单（REQ-053）：aria2 自建 fork raystyle/aria2 v1.37.1 五端落地验收。win 存量升级面（1.37.0 至 1.37.1）、linux/mac 新装面、各端 `aria2c --version` 探针、digest 锚校验留证、本仓门禁自跑。

## 结论

A 件（五端落地）**缺**：仅 lan-mac 过；wsl 与 lan-ubuntu 与 lan-linux 与 lan-win 四端装后探针全挂，同根因 **fork v1.37.1 资产非自含构建**。B 件（digest 锚校验）**过**：三平台锚逐字核等、镜像主通道全命中。C 件（门禁）**过**：九项退出码全 0（HEAD 80c7769）。

## 各端证据

| 端 | 面 | update 结果 | 探针 | 锚核（缓存资产 sha256 对 pin） |
|---|---|---|---|---|
| wsl | linux 新装 | 下载镜像过，装后报未找到可执行文件或无法读取版本（exit 2） | 挂：缺 libssh2.so.1 与 libcares.so.2（ldd 实证，libxml2/libsqlite3 本机有） | 44d59ee0a72d2237... 逐字等 |
| lan-win | win 升级 | 1.37.0 识别落后、下载镜像过，装后同报 | 挂：直跑 exit -1073741515（0xC0000135 DLL 缺失）；zip 内仅 aria2c.exe 一件，mingw 运行库未随包 | FFBC614B9E7F5FA5... 逐字等 |
| lan-mac | mac 新装 | action=installed version=1.37.1 | `aria2 version 1.37.1` 过，status 三态齐 | 装通即锚过（缓存件已被清） |
| lan-ubuntu | linux 新装 | 同 wsl 形态 | 挂：缺 libxml2.so.2（比 wsl 更精简的底座） | 44d59ee0... 逐字等 |
| lan-linux | linux 新装 | 同 wsl 形态 | 挂：缺 libxml2.so.2 | 44d59ee0... 逐字等 |

status 形态（四失败端一致）：locked=1.37.1、installed=-、path=true、exe 在位但不可运行。ark 的锚纪律与探针门工作正确（未放过坏件记 installed），失败面全在资产本身。

## 根因与去向

- 根因归 fork 构建面（ohmycloud/omc 域）：linux-gnu 动态链接系统库（libssh2/libcares/libxml2 随底座漂移）、win-gnu 缺 mingw 运行库 DLL。修复方向二选一：静态链接重构建（参照 typst D32 三平台单二进制形态）或随包带库加 catalog extract 落源适配。归总台裁。
- 附带损害通报：lan-win 旧 1.37.0 已被拆换（installed=-，旧上游资产不在镜像），修复版发布前该端 aria2 无可用件；linux 三端本无 aria2 无损。回执已提请总台重滚。
- 本仓零改动件裁定：catalog pin 与镜像与 seed 链均总台已绿，本仓不动物理面；ark 探针与锚门行为符合契约（R004 断言只写稳定字段同理），无需改码。

## 回执

经 herdr 通道回执总台（wR:p1），回执文本见本日 herdr 会话；commit 钩子即本笔。

## 验证

门禁九项（rumdl、mdcharlint、md-ref-scan、md-heading-scan、check.py、fmt、clippy、aidoc --check --strict、test --release --locked）退出码全 0，HEAD 80c7769。
