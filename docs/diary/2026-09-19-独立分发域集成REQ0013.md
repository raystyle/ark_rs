# 2026-09-19 独立分发域集成 REQ-0013

## 总台派单

install/update 通道集成自升级仓独立分发域（用户令「ark 要集成有单独域的 CLI 升级」）。五域（ark/hst/officecli/reader/browse，reader 自有桶 reader-dl 其余 env 桶多域），键形与边车不变；单一真源 = 云 catalog 节键 mirror_domain（总台并行滚入重签），仓内禁第二份域表；ark 自更新通道未用独立域保持不动。

## 实现

- catalog：Tool 新增 mirror_domain 可选键加 mirror_base() 访问器（非空即专属域、空串/空白/缺省回落 env.ohmygh.com）。
- resolve：pin_direct 镜像主通道 URL 基址取该键（mirror_url_at 显式基址形）。
- download：download_asset_with_mirror_at（install 主链与 npm-tgz 两腿接线 def.mirror_base()；docker/evergreen/selfupdate 无专属域键保持 env 域形不动）。
- 文档：R001 字段表、R015 消费侧、README 镜像源段、AGENTS 头行、--llms 下载行与 llms.txt（表述覆盖独立域事实、零内部编号）。
- 测试三层：单测独立域/回落/空白三态、集成测临时 catalog 带键 query url 断言专属域、实弹待云 catalog 键滚到后 update reader 取证。
- 踩坑：临时 catalog 集成测漏 exe 字段被 platform_managed 闸 skip（补 dir/bin/exe 三平台）；两枚 clippy（missing Errors 段与 too_many_arguments）随批收。

## 门禁与对线

230 测全绿；fmt/clippy（存量 13）/aidoc/md 四件套/PEVO 全绿。镜像瞬断抖动两见（env.ohmygh.com catalog 边车读超时，隔离复跑即绿，探活 200 三连），非本批回归。

## 改令重定向 REQ-0014 委托自升级

总台改令（用户裁 2026-09-19）：自维护自升级 CLI 走 CLI 自升级渠道、其余对齐 omc 维护 env。816a635 的下载腿接线撤回（mirror_url_at 消费、install 两腿、mirror_base 访问器；catalog 字段保留作 omc 键解析与 R001 契约）。新落 src/delegate.rs：四员委托命令路由（hst self update、browse update、reader self update、officecli __update-check__ 内部入口）加撤痕复痕舞蹈（家族仓零改动）加未装回落首装；cmd_update 接线 channel=self-update 机读面。实弹：update reader 委托链全通（家族自身双通道日志在证、失败如实 exit 1、落痕恢复），让位互锁正向证（reader 直跑被落痕拦）；成功路径证据已录：镜像恢复后 ark update reader 全链绿（INFO 撤痕、reader 自身「镜像通道取 reader-v0.9.0-...tar.gz + self_update: current 0.9.0」日志、[OK] 委托完成、exit 0、落痕 1.4.1 在位、status 与 --version 同报 0.9.0 即 pin）。action 诚实裁定面：家族成功且版本未动报 skipped（曾误报 updated 系 bin 未重编的假象，重编后验 skipped）。

## 封版 v1.4.2

REQ-0013 改向加 REQ-0014 委托批合一版；对线三轮（F1 至 F4 加 F-A/F-B 加记录面）全收 CONFIRM 放行；release.ps1 -Tag v1.4.2 -IncludePackages。

## 纪律落档 对线二犯同型教训

「批量脚本改文档/代码必须先落盘再断言后项」：8498d1d 与 f718e68 两笔同型——python 批量替换脚本把 write_text 放在全部断言之后，中途 assert 失败即整笔未落盘，而提交信息已声称改动内容（评审方以全仓 rg 实证两次抓住）。此后批量脚本纪律：每段替换后立即写盘（或先写后核），提交前用 rg 实证提交信息所声称的每个落点在树。

## 总台核收 REQ-0014 闭单 2026-09-19

D29 v1.4.2 核收过：tag/release/r2-seed（35426601600，误号 09 已知）自取核逐字对；catalog 48c2de8 滚 1.4.2；五端 self update 齐 1.4.2 实弹验版；hst 让位双报联动验收过（报A 直升被落痕拦指引走 ark、报B ark update hst 委托走通撤痕复痕零接触）。seed-assets 一次红为 CF 天气窗 runner 误判（dotnet/oscdimg CDN 拉败），复跑 123 全 synced，非本仓侧。

两条观察处置：a) 落痕内容滞留装时版本——已顺手收（stable 自更新成功后刷新部署目录 ark-managed 为远端现版；dev 无版本语义不刷留待装面幂等刷，入 Unreleased 窗）；b) 委托透传通道（wsl hst 吃 dev 段 2.5.0 领先 GitHub stable 2.4.0）属设计内，舰队 hst 面归总台与 hst 侧 stable 知会，本仓记档不动。
