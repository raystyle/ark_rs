# 2026-09-16 lan双端agent环境补装

## 派单与改令

ohmycloud 总台插队单（用户裁「先装」，优先于 aria2 复验单）：lan-ubuntu 与 lan-linux 两端补装 agent 环境（四家 agent、密钥配置同步、hst init 落面、装后验收）。执行中用户补令：装 agent 必须含 token 登录态，只装二进制不算过；凭据经 ssh 通道直传、值不进对话不进 argv 不进日志；装后以 `hst diagnose agents` 打真网关最小 token 作过件证据。随后用户改令「让总台设置token」：token 设置面转总台域（omc agent sync / n 注入域），ark 工位已完成的部分留用。

## 执行与证据

两端二进制面（ark install，catalog pin 对齐）：

| 端 | claude | codex | kimi | grok | hst | 备注 |
|---|---|---|---|---|---|---|
| lan-ubuntu | 2.1.270 | 0.154.0 | 0.42.0 | 1.0.13 | 1.3.0 | 五件已在位（总台并行轮已铺），探针全绿 |
| lan-linux | 2.1.270 | 0.154.0 | 0.42.0 | 1.0.13 | 1.3.0 | claude 2.1.268 与 kimi 0.39.1 落后，`ark install claude,kimi --force` 覆写对齐（单文件位 ~/.local/bin 无遮蔽） |

- lan-linux 顺补 pwsh 7.6.6（原不在 PATH，statusline 不渲染，违五端口径；装后 statusline.pwsh=found）。
- hst 面：两端 `hst init`（scope=full，yolo 先行纪律满足：端上未驻场任何 agent 会话）、`hst statusline` ok=true、doctor 四家 yolo 加 binary 加 hooks.form 加 statusline 全 ok（trust.* block 三条为家目录当项目根的位置形态，非凭据缺口）。
- diagnose agents 两端输出与总台基线逐字同款：codex key=alive（deepseek 真网关最小 token 打通）、claude base_url 空（总台同形态，claude 网关别名与 key 走 spawn 时 env 注入面非文件面）、ok=true。
- 凭据面：lan-ubuntu 在改令前已从总台通道直传文件级登录态（claude settings.json、codex auth 加 config、kimi config 加 device_id 加 credentials、grok auth 加 agent_id 加 config，tar 流只有路径进 argv）；lan-linux 缺 kimi credentials 与 grok auth.json，连同 claude 网关别名注入整面转总台 token 轮（已派单 wR:p1，总台处理中）。

## 边界遵守

未动 aria2（队列单保持）；密钥值零进对话零进 argv 零进日志（只操作文件路径与探针输出）。

## 总台回执合流

总台 token 面收口回执（用户转达）经独立核证：

- ohmycloud commit 3363ff6（feat: lan-linux入agent端点矩阵）自取实证，与陈述一致；lan-linux 此前确缺该端矩阵位。
- 总台 token 面按 REQ-049 矩阵覆 claude/codex：两端 doctor 全绿（configOk/envAgeOk/tokenPresent 全 true）、两端 hst diagnose agents ok=true 且 codex key=alive，与 ark 工位实测同构；claude base_url 空为总台同构形态。
- **开放项**：lan-linux 的 kimi credentials 与 grok auth.json 两件仍缺（omc agent doctor 矩阵不含 kimi/grok，总台 token 收口面不覆盖；lan-ubuntu 两件已在位）。凭据分发边界经用户改令「让总台设置token」划归总台域，ark 工位不越界补位，已在合流回执中列明请总台裁量（补齐或明示豁免）。

## 回执

回执发总台 wR:p1：A 过（两端五件 pin 对齐探针全绿）、B 过（总台 token 面 claude/codex 实证核验；kimi/grok 文件面 lan-ubuntu 在位、lan-linux 两件开放归总台裁量）、C 过（hst init 加 statusline 加 doctor 全绿，pwsh 缺口已补）、D 过（diagnose agents 两端与总台基线同构、codex key alive）。本笔即 commit 钩子。

## 闭环终态

总台终回执：开放项已闭（lan-linux kimi credentials 加 grok auth.json 两件经 ssh 加 scp 直传就位，perms 600，值零落地）。如实记档两条：

- 总台源 kimi credentials 本身是 revoked tombstone（access_token 空，全舰队同病含总台），两端复制仅为形态对齐；kimi 真活需用户重登（kimi login 交互件），总台已作舰队级开放债记录，非本单缺陷。
- grok auth.json 为过期加 refresh_token 形（下次运行自刷新，与总台同态）。

「先装」插队单全件闭环：A 过、B 过、C 过、D 过；合流回执已投总台 wR:p1。kimi 重登债待用户择时处理。

## 验证

本轮零码改；md 四件套随本笔复跑全 0。九项全量门禁当日基线在 80c7769 全 0（REQ-053 C 件同轮实证）。
