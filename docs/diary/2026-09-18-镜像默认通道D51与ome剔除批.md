# 2026-09-18 镜像默认通道 D51 与 ome 剔除批

## 总台派单

镜像优先转正为默认通道（飞轮正式单，用户两连发：「ark 为什么不是默认使用 ohmygh.com 获取元数据和下载！github 应该是兜底，不是默认流程」「各仓的自更新应该也是默认 ohmygh.com」）。实证病灶：五端 update/install 全量逐工具打 api.github.com，gh 未认证或凭证失效撞匿名 60 次/时配额（lan-win 30 项失败 exit 1、lan-mac 401、lan-linux 未 login、lan-ubuntu 无 gh）。

用户加令（批中）：「剔除更换 ome 这类关键字为 ark」。总台追加单：封新版令（browse/ark 两仓封新版，本仓 minor 升位，先收口镜像单再封）。

## 镜像默认通道 D51 与 ADR-0008 与 REQ-0010

三面翻转：

- 解析面：resolve_github pin 驱动（无 latest/tag/version 选项）零 GitHub API。新增纯函数 pin_direct：catalog pin 三键（tag/version/asset）在位即镜像资产域 URL 主通道、`Resolution.fallback_url` 承载 GitHub release 确定性下载直链（对象直链非 API）兜底；install 主链与 npm-tgz 两处下载调用官方回落地址改取 fallback_url（docker 三处走 cdn 官方链不变）。D38「API 失败回落镜像」闭包删除（被默认通道收编）。pin 三键缺一或显式选项才打 API。
- 下载面：D44 机制不动，核对无回归；新增 ARK_MIRROR=0 官方优先逃逸阀（跳过镜像首试直走官方，is_mirror_off 纯核入测）。
- 自更新面：元数据镜像段读序先行（mirror_meta 核心提取）、GitHub API 兜底、双败报两段错误；ARK_MIRROR=0 反转读序（官方先行镜像回落）。=1 转正默认兼容 no-op。
- update 语义对齐：cmd_update 解析改 pin 驱动（ResolveOptions::default），云端「最新」定义改指镜像与 catalog；D49 三态与不回写锁定不动。私有仓工具（omc）update 从必败（latest 打 API 404）变镜像直装可用。

设计决策三枚（ADR-0008）：latest 保留上游最新语义（镜像无 latest 元数据，临时钉版工作流依赖）、上游新于 pin 不跟进（滚锁归数据面）、ARK_MIRROR=0 逃逸阀/=1 no-op。三问入回执待用户裁。

测试面：pin_direct 三件单测（镜像主通道 URL 形、显式选项不直装、pin 缺键无资格）、is_mirror_off 值域、离线集成测 query 零 API（断网面 + url=env.ohmygh.com 直证）。

## ome 关键字剔除批 用户令

边界裁定：活体面全量 ark 化，冻结历史档（PRD/diary/ADR 历史）与被签内容对逐字保留。

- env 旧名读回全撤：env_var_or(primary, fallback) 改单名 env_var（OME_*/OHMYENV_ROOT 读回链删除，读回语义测试改写单名语义）；fleet 侧需确认无旧名残留（回执知会）。
- selfupdate 兼容层收口：asset_compat_for_this_platform（ome-<triple> 名）删除、镜像 ome/ 段读序腿删除（mirror_attempts 四层改三层，段恒 ark/）、extract ome-self 双接受收口（仅认 ark-self；云端权威 catalog 2026-09-14 起写 ark-self，长期未同步端 `ark catalog sync` 即迁）、fallback_seg 删除。
- 标识符：is_ome_self 改 is_ark_self、remove_ome_alias 改 remove_legacy_alias、ome_alias_target 改 legacy_alias_target、omerr.rs 改 arerr.rs（OmeError 改 ArkError）、main.rs OmeSelf 变体改 ArkSelf、测试助手 ome() 改 ark_cli()/ark_reg()、UA 串 ome-catalog/ome-bootstrap 改 ark-*、临时目录与夹具串全量 ark 化。
- seed 脚本：EVERGREEN_EXTRACT 去 ome-self、seed-inventory 旧数据位读回行撤除。
- 保留实称（on-disk 事实，非关键字）：EnvRoot 磁盘目录名 ohmyenv（迁移涉五端数据，另立项待裁）、旧部署位 Programs\ome（清理目标路径本身）、镜像自检签名被签内容对（私钥在 ohmycloud 侧，改串须持钥重签；首版改串即验签红，当场回退并注明）。
- 踩坑：SELFTEST_MSG 改名即验签失败（被签内容对不可改），回退保绿。

## 门禁与验证

- cargo fmt --check 绿、cargo clippy --release --locked exit 0（仅存量 docker.rs cfg 门控死码告警）、cargo test --release --locked 217 项全绿。
- cargo aidoc 重生成 27 件 + --check --strict 绿（omerr.md 删、arerr.md 新）。
- md 四件套绿（rumdl 114 文件、charlint、ref-scan 136 文件、heading-scan）；PEVO check PASS 11/SKIP 1（豁免在册）。
- 数据核验：本机同步副本 53 工具中 44 个 GitHub 分支工具 pin 三平台全量齐备（pin 驱动零 API 无数据缺口）、cloud catalog extract=ark-self 单值。

## 封版 1.4.0 与第三单

- 第三单 cli-docs 采纳（REQ-0011）：对照表全件、裸调用面 exit 0、漂移守卫、README 四节重构；旗标七件与信封按 R013 冻结记不适用。
- 封版 1.4.0（minor，总台 wave 令）：D51 批加剔除批加 cli-docs 批加 v1.3.0 补审批合一版；release.ps1 -Tag v1.4.0 -IncludePackages（REQ-0008 双挂窗形）。

## 供给表对齐批 用户指正

- 用户指正 README 残留 browser-harness：云端清单实况核对（ark catalog sync 后 53 工具），browser-harness 已出册、reader 已回册、browse/officecli/oxvg/resvg/agent-svgtools 新入册。README 供给表重建（命令工具 27、删运行时衍生行、计数 50 改 53 四处）、AGENTS/llms/R001 计数同步、real.rs reader 出册断言翻回册（roster 真源在数据面，测试不冻结名录）。

## 封版后收尾

- v1.4.0 发布回执（总台独立复核）：tag 指向与 release 12 资产与 r2-seed success 过；main build macos 岗红一面（D51 新测 age_fixture 漏 mac 平台键，总台定性请求）。定性：测试病非码病（本机 linux 跑绿未暴露夹具缺 mac_*；pin_direct 在 mac 正确要求 mac_* 键，真实 catalog 三平台键齐备）；修复 4aebe72 补齐 mac 四键推 main，mac CI 绿即证。数据面三件由总台随放行恢复。
- herdr 知会 ohmycloud 已投递（wR:p1 agent_prompted，D29）。
- 总台转告：omc 侧 2026-09-14 起 workflow 只发 ark/catalog 六对象（ome/catalog 停写是事实）；桶内存量清理待用户另批；本仓 cloud_catalog_keys 兼容读键保留（不依赖桶内对象存在），G1 收口随下批。

## 待办与回执

- 回执两单：镜像单（REQ-0010/ADR-0008/commit sha/三门禁/行为对照表/待裁三问）与封版单（进行中：minor 1.4.0、release.ps1 三段式、tag+release、播种、herdr 知会 ohmycloud）。
- 对线一轮（ark-codex-review）：F1 至 F4 全修（文档四处矛盾、selfupdate 第二腿同 URL、checksum 边车源、query size 口径）、G4 至 G7 修、G1 镜像键面另批（知会 omc）、G2/G3 记 ADR 与回执；二轮快核后放行。
- 对线三轮（ark-codex-review，herdr 右格驻场）：一轮 F1 至 F4 必修加 G1 至 G7 建议（a13833c 全修 F 与 G4 至 G7，G1 另批知会 omc、G2/G3 记 ADR 与回执）；二轮 F1 三行文档加 G-a 四条（a0d761a 全收：MetaPick 记源、api_err 短路、双败两段错误串、上报随实际命中件）；三轮 F1 manual_inspect 告警一行修（放行裁定：改完即推）。三轮 CONFIRM 面覆盖 pin_direct 资格边界、fallback_url 形态与平台面、逃逸阀自洽、update 回归面、ome 机械面清干净度。
- 五端舰队实弹复跑 update 全量验证零 API 实效（总台安排）。
