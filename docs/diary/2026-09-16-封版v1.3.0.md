# 2026-09-16 封版v1.3.0

## 判据与封版件

用户令封版。semver 判据（flow-release 第七节，写 REQ-0006）：D50 为命令面行为变化（子命令撤除加发现层契约增强）取 minor，1.2.3 至 1.3.0。封版件：Cargo.toml 加 Cargo.lock 版本、CHANGELOG 转版（skill 面撤除里程碑加对线修正批两条）、ROADMAP 八阶段行补 1.2.3 与 1.3.0、AGENTS 当前阶段句切 v1.3.0、REQ-0006 立项回填、本日记。

## 推送前对线

ark 工位自建评审格（w11:p2，herdr pane split 右分 40% 后 herdr agent start ark-codex-review --kind codex；用户级 codex yolo 先行在位）。两轮：

- 一轮（1f0720d 全 diff 快核）：无 F，可推 main；G 五条（G1 manifest 表行转义口径不一致、G2 exists() 吞错、G3 清理面无自动化断言、G4 旧位口径缺注记、G5 头行计数漂移）。G 批全采纳为 commit 9a065ea：G1 裸管道改 \\| 统一；G2 换 symlink_metadata 三态（NotFound 静默、其余上抛，评审确认顺修悬空符号链接漏删旧漏）；G3 抽 remove_legacy_skill_at 可测核加三态单测（在删 true、不在 false、目录占名 Err）；G4 doc 补旧位语义两句；G5 头行摘数字去计数（计数权威归云端 catalog）。
- 二轮（9a065ea 快核）：G 批逐条 CONFIRM（评审方独立跑 cargo test --lib remove_legacy_skill 一过），终审 CONFIRM 放行推 main 封版。

## 验证

- 本机门禁：fmt 与 clippy 与 test --release --locked 全绿（11 套件）、aidoc 再生成 --check --strict 干净、md 四件套全 0。
- tag 首推（dc9ad5c）macos 岗红：REQ-0003 两测（自管位系列）合成夹具两支结构把 mac 落进 linux 支，而 platform_managed 在 mac 只认 mac_exe，走空态跳过致期望面落空。修复一（0886beb）补 mac_exe 支路；复跑仍红于 resolve 段（mac 面缺 mac_version，pin 比对与 cdn 版本源双双落空），修复二（d913122）补 mac_version。tag 两度重指（未出 release 无回退问题，D48 先例），第三轮 run 35116504682 四岗全绿（linux 与 win-gnu 与 macos 与 mirror-r2）。今晨 b6d4a8a main run 同因红，本轮随修转绿。
- 评审格快核测试修复：CONFIRM（机械改动无误、与 platform_managed 口径一致、其余两支用例安全）；G 一条可选遗留（夹具恒写三平台齐套防「本地看不见别支」盲区，下一笔）；旁注归 omc 数据面（zoxide 与 sheldon 与 fnm 有 mac_exe 无 mac pin 四键，mac install 落「尚未 pin」Err，另批核）。
- 资产验收：release v1.3.0 三平台裸二进制（与 1.2.2/1.2.3 同款形态，sha 锚在镜像边车域）；linux 资产下载实测 sha256=18c657eaeafa2e52...，--version 1.3.0、--llms 新头两纪律行在、skill 拒退出码 2。
- self update --stable 报 action=current：判新走 catalog 锚（ark pin 仍 1.2.3、本机 1.2.3），行为正确；镜像 stable 段边车仍锚 c6606351（1.2.3）、版本段 ark/1.3.0 未播，均待 omc 滚 pin 与 seed，已入 D29 知会单。

## D29 知会

发布后 herdr 知会 ohmycloud 总台（资产与 digest 清单、catalog 滚 pin 请求、镜像 stable 段切换、omc 侧旁注），舰队 self update 随 omc catalog 滚动收敛；D50 面舰队效果：新二进制 init 幂等清理数据目录旧 SKILL.md（五端已一次性清扫在前）。
