# 2026-09-16 封版v1.3.0

## 判据与封版件

用户令封版。semver 判据（flow-release 第七节，写 REQ-0006）：D50 为命令面行为变化（子命令撤除加发现层契约增强）取 minor，1.2.3 至 1.3.0。封版件：Cargo.toml 加 Cargo.lock 版本、CHANGELOG 转版（skill 面撤除里程碑加对线修正批两条）、ROADMAP 八阶段行补 1.2.3 与 1.3.0、AGENTS 当前阶段句切 v1.3.0、REQ-0006 立项回填、本日记。

## 推送前对线

ark 工位自建评审格（w11:p2，herdr pane split 右分 40% 后 herdr agent start ark-codex-review --kind codex；用户级 codex yolo 先行在位）。两轮：

- 一轮（1f0720d 全 diff 快核）：无 F，可推 main；G 五条（G1 manifest 表行转义口径不一致、G2 exists() 吞错、G3 清理面无自动化断言、G4 旧位口径缺注记、G5 头行计数漂移）。G 批全采纳为 commit 9a065ea：G1 裸管道改 \\| 统一；G2 换 symlink_metadata 三态（NotFound 静默、其余上抛，评审确认顺修悬空符号链接漏删旧漏）；G3 抽 remove_legacy_skill_at 可测核加三态单测（在删 true、不在 false、目录占名 Err）；G4 doc 补旧位语义两句；G5 头行摘数字去计数（计数权威归云端 catalog）。
- 二轮（9a065ea 快核）：G 批逐条 CONFIRM（评审方独立跑 cargo test --lib remove_legacy_skill 一过），终审 CONFIRM 放行推 main 封版。

## 验证

- 本机门禁：fmt 与 clippy 与 test --release --locked 全绿（11 套件）、aidoc 再生成 --check --strict 干净、md 四件套全 0。
- tag v1.3.0 推送后 CI 与资产验收（run conclusion、资产件数、抽查三件）回执总台并回填本节。

## D29 知会

发布后 herdr 知会 ohmycloud 总台（资产与 digest 清单），舰队 self update 随 omc catalog 滚动收敛；D50 面舰队效果：新二进制 init 幂等清理数据目录旧 SKILL.md（五端已一次性清扫在前）。
