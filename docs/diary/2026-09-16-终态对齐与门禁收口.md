# 2026-09-16 终态对齐与门禁收口

## dev-evo v0.6.0 终态清单自审

- 骨架面全过（五节合同、文档地图、ADR/REQ 索引一致、红线注记）。
- 门禁面：check.py 12 PASS 退出码 0；PE-11 存量已在前批清剿归零，PEVO_CHECK_ALLOW 豁免机制本仓不适用（活跃面零容忍）。
- 档案面：本笔即当日日记；research S001 至 S008 编号与 README 索引在位；proven 留档理由在文档地图；mistakes 已升 ADR-0002 至 0006。
- 三栈面：AGENTS Commands 补 fmt 与 clippy；missing_docs 与 cargo test --doc 裁定不适用（CLI 仓 bin 面、对外契约走输出面文档）。

## 验证

check.py 12 PASS；四件套绿。

## aidoc 投影强制化续

- dev-evo 第五十九批（ADR-0006）撤换本仓三栈不适用裁定：160 项 pub 缺注清零（catalog 88 字段按 R001 语义映射批写、其余逐条），missing_docs deny 入 Cargo.toml lints，cargo aidoc 生成 26 件投影进 docs/aidoc/，--check --strict 入 AGENTS Commands。
- 生成物豁免三面：PEVO_CHECK_ALLOW 指向 docs/aidoc/ 在册、rumdl exclude、mdcharlint 排除集（顺修其排除逻辑只看倒数第二段对嵌套目录失效的坑，改任一段匹配）。
- 曲折：批写脚本断言断链三次（锚错文件、重复字段、映射先中 CloudCatalog），教训同 M006 家族：批处理脚本应先 dry-run 打印计划再落盘。

## 封版 v1.2.3

统一封版令（总台，七仓首批到齐）。版本 1.2.2 到 1.2.3（Cargo.toml 加 Cargo.lock）；CHANGELOG Unreleased 归 1.2.3（patch：舰队对线修复批加 dev-evo 治理对齐批）；ROADMAP 阶段注记。验收：tag run conclusion 自取，资产与 digest 清单回执总台。

## 开发仓位迁移

本机 WSL 十仓完成独立 VHDX 迁移：D:\wsl_workspace\repos.vhdx（50GB ext4）挂载 /mnt/wsl/repos，快捷 ~/repos；登录自动挂载已注册（WSL-RepoDisk schtask）。ark_rs 新工作根 ~/repos/ark_rs；AGENTS 跨仓路径已切 ~/repos/ 形（98d67d0）；旧位 ~/ark_rs 保留一至两天过渡后裁。本笔起日记与提交均落新位。

## 封版尾巴收口

状态核对发现两处封版遗留：CHANGELOG 的 D49/bug4/temp PATH 详目仍挂 `[Unreleased]`（封版实际只插了 `[1.2.3]` 摘要行，与本日前笔「Unreleased 归 1.2.3」记述及 1.2.2 封版形态不符）；AGENTS 环境节当前阶段句滞后一版（v1.2.2）。本笔收口：详目并入 `[1.2.3]` 节、`[Unreleased]` 残节删、环境节切 v1.2.3 口径（Unreleased 窗空）。

## 五端产物验收轮

编译产物后五端直测（用户裁定避开宿主 interop 负载：lan-win 走 127.0.0.1 回环 ssh 加 pwsh，四端 ssh 直达）。产物矩阵：WSL 与 ubuntu 与 linux 共用 x86_64-linux-gnu（本机编译，glibc 2.39 不高于端上 2.43 直拷）；宿主 x86_64-pc-windows-gnu（WSL mingw 交叉 2m45s，D46 链）；mac aarch64 原生编译（端上 rust 1.97.0，rsync 源码排除 target 与 git）。HEAD 与 v1.2.3 差异纯文档三笔，产物与 release 资产功能面等价。

- 回归：`cargo test --release --locked` 全绿（退出码 0，含 real 门控面）。
- 验收：五端 `--version` 与 `query jq` 与 `catalog status` 与 `status` 与 `doctor` 全通（doctor 的 broken 与 version-drift 为各端真实环境结论非产物故障）；幂等 `install jq` 五端 skip（宿主 `action=skipped` 明示）；`status` 不收工具参数（验收中造错用法被 clap 拒，行为正确）。宿主通道引号坑两犯（cmd 层吃管道与 `\$` 转义），终态姿势 pwsh `-EncodedCommand`（UTF-16LE base64），零引号嵌套。
- AB（1.2.2 release 资产 vs 本地 1.2.3）：bug4 逗号串 1.2.2 整串当单名拒收（`未知工具: foo,bar`）、1.2.3 拆分逐工具（`未知工具: foo`）；D49 漂移三态用本机 hst 真场景（locked 1.3.0 installed 1.2.0）：1.2.2 `action=skipped`（旧判据挡装实锤）、1.2.3 进真装。
- 发现一（供给缺口，归 omc 数据面）：hst 1.3.0 双链失败：镜像桶 404（未播属常态）加官方资产 sha 与 catalog 锚不符（linux 期望 3301F6 实际 DB4B8D、win 期望 464BED 实际 AA2323，双平台复现），锚拦下本机保持 1.2.0 零污染；旧版常年 skip 掩盖此缺口，D49 真装才暴露。待 omc 滚锚或核对资产重传。
- 发现二（本仓 bug 候选）：doctor 的「检测到缺失」HINT 列平台不适用工具（宿主 shellcheck 与 sheldon 无 win exe 字段仍进清单，update 面如实跳过），提示与执行面口径不一致，bug4 同族。
- 发现三（自洽确认非 bug）：git 实装 .windows.4 锁 .windows.5，status 明细如实显示但不产 HINT、update 报已是最新 skip，两面同按 pin_drift 数值段口径（尾二修 `.windows.N` 建构号有意忽略），自洽。
- 总台转 reader_rs REQ-050 两件处置：宿主直清可清项为零（claude 原地纳管归 agent 自更、git 口径判等、grok 领先归滚锁、hst 卡供给、lightpanda 与 resvg 与 shellcheck 与 sheldon win 无锁、browser-harness 有锁卡 node 前置而 fnm 在位 node 未装）；bootstrap CF 滞后研判判优 **sync 拉取带 cache busting** 优于 init 收尾二次 sync（reader_rs 同端 curl 带 `?t=` 击穿即得新 pin 为直接实证；二次 sync 在边缘滞后窗内大概率同旧；D45 三犯同族；落码须 sha 边车与 toml 本体同 bust 防异源错配，seq 门与 minisign 校验照常），落码另起轮（立 REQ 加对线后推）。
- temp PATH 闸 POSIX 真机面未强测（沙盒 install 走 skip 通道未触注册面），win 注册面回归靠「不再新增 Temp 注册面」观察，单测面已盖。清理：四端临时面已清，本地 /tmp/ark-ab 随本轮收尾清。

## typst 全平台命令分发派单

A 至 D 四件全过、零代码改动（catalog 驱动全覆盖，「缺则补」为零件裁定留痕）：

- A 命令面：sync（origin=cloud）与 query（tag=v0.15.1 加 asset=typst-x86_64-unknown-linux-musl.tar.xz）与 status 三态与 doctor（typst 无 FAIL/HINT 条目，version-drift FAIL 为 hst 等真漂移项贡献）对 typst 全通。
- B 五端探针：宿主 `typst 0.15.1 (9dfd3a08)` PATH 在位；lan-linux installed=0.15.1（ssh 非登录 PATH 不含 `~/.local/bin` 的探针假象，绝对路径复探实证）；WSL 与 lan-mac 与 lan-ubuntu 与 lan-linux2 installed=- 待装；lan-linux2 首探在局（Linux x86_64）。派单文案「REQ-049 既有裁」为本仓不存在编号（实况 REQ-0001/0002，邻仓误植），如实留痕。
- C 版本对齐：四端 `ark update typst` 全部 `action=installed version=0.15.1 dir=~/.local/bin`，digest 锚校验全过无回落告警；五端终态 `typst --version` 全等 `0.15.1 (9dfd3a08)`（build hash 一致）。
- D 门禁退出码：fmt=0、clippy=0、aidoc --check --strict=0、cargo test --release --locked 本日退出 0（见五端轮）、md 四件套与 check.py 本笔随提交复跑。
- 回执经用户面人工转总台（本工位无 wR 通道）。
- 更正上行为时未识通道：herdr 控制面可用（本会话在 Herdr 管理内，HERDR_ENV=1，wS:p1），`herdr agent prompt wR:p1 <文本>` 即 R014 通道更替后的跨仓周知正姿，typst 回执与 hst 缺口例外通报与 check.py 路径迁移三件已按此投递总台（agent_prompted 回执，R014 herdr 通道本工位首验）。
- 门禁曲折一笔：diary 新节标题带括号与两处破折号被 md-heading-scan 与 PE-11 当场拦下（本笔即改）；dev-evo check.py 路径随 project-evo 第六十六批 skill 形态重排（ProjectEvo 仓名退役、dev-evo 并 evo-adr 的 code-kit）双重过期，AGENTS 骨架合规句已切新路径。

## hst 供给缺口闭环与宿主通道异常

总台回执三件（typst 收讫、hst pin 已修 636069a 加 seed run 35075695896 双绿加镜像 db4b8d 逐字等、check 路径 ohmycloud 已切 f3193f7）后，端上闭环验证：

- WSL：resync manifest seq 53 新册到手；本机 1.3.0 已在位（疑 hst_rs 工位顺带装，update 走 skip 正确判据），`install hst --force` 补完整链实证 `action=installed version=1.3.0`（新锚下载校验解压全通）。
- lan 四端（mac 加 ubuntu 加 linux 加 linux2）：resync 后 locked 全 1.3.0，`update hst` 全部 `action=installed`，`hst --version` 四端 1.3.0（mac aarch64 与 linux x86_64 双平台新锚下载链端上实证，缺口闭环）。
- 宿主：回环 ssh 突发 `Permission denied (publickey,password,keyboard-interactive)`（此前全天 BatchMode 免密通，间隔重试两连拒非瞬态，sshd 在跑认证层拒，疑 authorized_keys 或 sshd 配置被动），宿主 hst 未装面挂起，归舰队侧排查，通报总台。

## 宿主通道恢复与 hst 口径差澄清

总台修复回执（路由面 ssh config lan-win 切 127.0.0.1 加钥匙面 administrators_authorized_keys 满编去重五行）后补验：

- 通道全通：`ssh lan-win` BatchMode 免密（whoami=ai-lab\ray），catalog resync seq 53 新册到手。
- hst 口径差真相：总台探针 `hst --version`=1.3.0 命中 `C:\Users\ray\.hst\bin\hst.exe`（hst 自管部署位，其 self-update 通道）；ark status `installed=- path=false` 如实（ark 只认 EnvRoot 注册位）。两把尺子量两个位，两边都对；前笔「宿主 hst 未装面挂起」表述不严谨，应为「EnvRoot 面未装、PATH 面自管位已在 1.3.0」。
- 终态裁定：宿主 hst 走自管位（claude agent 自更面同族），ark 不重复装 EnvRoot 副本。
- 宿主 `update hst` 卡解析面：GitHub API 直连失败加 gh 匿名限流三连 403（宿主网络当日态），未到下载步；非锚问题（新锚链 WSL 加 lan 四端已实证）。
- 引擎改进点记档（bug 候选族）：「PATH 存量原地纳管跳过」INFO 面只覆盖 claude 特判形态，hst 族自管位工具漏纳管（update 不识别 PATH 存量直接走 resolve 装流程），待立 REQ 统一纳管判据。

## REQ-0003 与 REQ-0004 实现批

- 发现二更正撤销：doctor 的「检测到缺失」HINT **有**平台过滤（判据 `exe.is_some()` 即本平台字段），宿主复跑 HINT 仅 `hst,browser-harness,omc` 三项（shellcheck 与 sheldon 不在）；前笔把 update 面「平台不适用」的观察错安到 doctor 面，属误报，更正如上。
- 五端口径修正（用户裁）：**五端 4 机 = WSL 加 lan-win 加 lan-mac 加 lan-ubuntu 加 lan-linux**，是全平台测试基建；lan-linux2 为编外纯服务开发机（EXEMPT 自判不适用留痕），typst 与 hst 轮曾把它当第五端操作（装的动作本身无害、装的是最新版），回执口径以本笔为准更正。
- REQ-0003（PATH 存量已达锁定版纳管，update 面）：main.rs 在 agent 特判（D07）后、resolve 前加纳管分支（EnvRoot 未装且 PATH 命中且 PATH 探测版本等于 pin 即 INFO 纳管跳过）；**install 面收窄裁定不加**（install 是「显式装入管理面」意图，PATH 同版不拦；linux_install 真机闭环两测曾挂即此语义冲突的哨兵信号，撤分支后复绿）。
- REQ-0004（权威边车拉取时间戳击穿）：resolve_cloud_sha 的权威边车拉取由裸 URL 补 `t=<unix秒>`（抽 authoritative_sidecar_url 纯函数入测）；probe 面与双签名件面本就带 t，唯此跳漏（cloud_sidecar_url 注释的设计意图未落实处），即 reader_rs REQ-050 bootstrap 旧 pin 形态的根因行；本体 `?v=sha` 锚键控不变量不动。
- 测试曲折（R001 平台字段族三犯同型，测试条目构造高频坑）：沙盒 catalog 的 POSIX 条目要 `linux_exe` 加 `linux_cdn_url` 加 `linux_version`（顶层 exe/cdn_url/version 在 Linux 均不读），另犯 TOML 重复键（version 叠写）与测试函数名含 PATH 大写触 non_snake_case。
- 验证：cargo test --release --locked 全绿 204 项（新增 update 纳管跳过与 install 穿透两用例、权威边车 url 单测）；fmt=0 加 clippy=0 加 aidoc 重生成后 --check --strict=0。
- 对线：herdr 驱动 codex（wS:p2 ark-codex-review）审四件改动，结论见后笔。

## 对线结论与修复回执

codex 六点（deepseek-v4-flash high，6m28s，自带实证跑测与源码考古）与处置：

- 【中·改】纳管分支初版插在 agent 特判后（自管与 rustup 与平台判定之前），会截获 tools.ark 自管条目：PATH 有同版 ark 时 `version=1.2.3` 翻转既有 `version=self-managed` 机读契约且 INFO 建议 `install --force` 对自管条目无效。处置：分支挪到 hold 判定后、resolve 前（hst 普通条目照命中）。
- 【中·撤】REQ-0004 原 RCA 不成立：fetch 层 `mirror_sidecar_anchor_fast`（996d342，2026-09-08 起）内部已带 `t=` 击穿，call-site 裸 URL 只是入参形态；错误串印入参裸 URL 正是「此跳没穿」误判的来源。处置：call-site 双写撤除、authoritative_sidecar_url 与单测撤、REQ-0004 转 rejected；顺带修 `parse_sidecar_sha` 错误串印实际请求 URL（防同型误判）；bootstrap 旧 pin 真根因待定（候选：CF 缓存键忽略 query 或 R2 覆写传播滞后，归 omc 域配置排查另立 RCA）。
- 【中低】update --force 初版被纳管静默忽略。处置：分支加 `!force`（force 走真装）。
- 【低】探测成本与口径：EnvRoot 判存改 `exists` 短路不跑版本子进程；判据从字符串等值改 `pin_drift(...)==Current`（与 D49 同尺，防 v1.3.0 与 1.3.0 形态差异静默失效）。
- 【nit】install 穿透用例补失败点锚定（`example.invalid` 断言）；22.7s 真下载链成本接受（哨兵价值）。
- codex 附记：diary 在其对线期间被本工位并行写入属正常（同会话两轮）；/tmp 留 arkrev. 与 arkhome. 沙盒三件为其复现临时物。

验证（修复批后全绿）：cargo test --release --locked 203 项 0 失败；fmt=0 加 clippy=0 加 aidoc --check --strict=0（重生成投影四件）；rumdl 加 mdcharlint 加 md-ref-scan 加 md-heading-scan 加 check.py 全 0（途中 REQ-0004 文案两处 em dash 禁字被拦即改）。
