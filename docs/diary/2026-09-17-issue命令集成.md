# 2026-09-17 issue命令集成

## 派单

总台对齐单（issue 命令集成，REQ-057 契约）：统一 issue 入口 issues.ohmygh.com 已上线（Worker 加 D1 真源，每 IP 10 条/时），各仓集成 issue 子命令（new 一键提交自动带 tool=ark 与版本/平台/host；list/show 读面）；agent 使用纪律入仓合同；回执附实弹证据。omc 为参考实现。

## 实现：单笔 7e04cf2，三轮对线至 CONFIRM

- src/issue.rs 新模块：契约校验（tool 形加 title UTF-16 边界加 body 上限加三字段客户端截断，与 REQ-057 validateIssue 语义对齐）；ureq 客户端面三叶（post/list/show）；零新增 crate
- main.rs：issue 子命令三叶（catalog 前早期派发，无清单环境可反馈）；--llms 清单行与 EX_ISSUE 示例
- AGENTS（Commands 报缺陷行加 Must 遇缺陷一键反馈纪律）加 README（issue 反馈节）加 REQ-0009 立项 implemented 加 R013 冻结表补行

## 对线三轮实录

- 一轮 1F 加 4G：F1 是真缺陷（ureq 2 缺省把 4xx/5xx 当 Err(Status)，read_json 非 2xx 分诊是死码，评审起 mock 实测 429/500 被误报「服务不可达」），修 unwrap_http 还原助手，工位自起 mock 复演文案透出；G1 裸子命令退 2、G2 计数改 UTF-16 语义（服务端 JS .length 同义，emoji 计 2）、G3 R013 补行、G4 limit 夹取加 serde default 加 timeout 注记
- 二轮 1F：我给 --timeout 写的「0 = 不限时」与 ureq 实义相反（ZERO 是即超时，评审实测 0.003 秒红），修采建议 (a)：0 时不设整体超时；残余 clip 混用语义一并收（clip_utf16）
- 三轮 CONFIRM 加 G 顺采（clip_utf16 补 emoji 截断回归测，151 测绿）
- 插曲：共享评审格被 browse-rs 工位并行派单插队，我方二轮被弃半途无回执，续跑指令收回；跨仓收件格并发派单的插队坑实录（回执按 commit sha 甄别的纪律正好用上）

## 实弹证据

- #7（首弹）：host=unknown 抓出真缺陷，非交互 shell 不导出 HOSTNAME，agent 场景全中；修 host 取值梯（HOSTNAME env 加 COMPUTERNAME env 加 hostname 子命令兜底）
- #8（复弹）：env -u HOSTNAME 场景 host=AI-LAB（WSL 承宿主名）；list --tool ark 双条可见、show 全字段回读一致，退出码全 0
- 读面另证：mock 429 带「rate limited: 10/h per IP」、500 带「d1 unavailable」文案透出

## 验证

lib 151 测全绿（含 emoji 边界与截断回归）；fmt 与 clippy（issue 面零命中）与 aidoc --check 与 md 四件与 PEVO 全 0；实弹双发与读面复核对账。本笔即 commit 钩子。
