# PRD 需求清单冻结索引

> **冻结声明（ADR-0001，2026-09-15）**：本表 D01 至 D49 为历史决策冻结索引（只读，状态可查不再流转）；D50 起不可逆选择立 `docs/adr/` ADR 详篇，本表只追加指针行；新需求立 `docs/requirements/` REQ。

> 角色：需求清单，四原语之首，需求驱动目标。GOAL 的每个目标应能回指本清单条目。
> 分工：PRD=要什么；GOAL=要达成什么；PLAN=怎么做；TODO=做到哪。
> 本清单于 2026-09-05 补建（evo 骨架重建）：D01 至 D05 按 GOAL 历史与 diary 时间线追溯登记，均标 [推断]；此后新需求走追问链逐条澄清后登记。

## 需求清单

| 编号 | 需求 | 状态 | 澄清轮次 | 派生去向 |
| --- | --- | --- | --- | --- |
| D01 | ome 立项：自 ohmypwsh 的 `ohmyenv.ps1` 剥离为本机跨平台环境部署管理 Rust CLI，Windows 域八命令（query/pin/install/deploy/update/status/daily/self-deploy）可用 | 已交付 | [推断] 追溯登记（2026-09-05） | GOAL 历史 2026-08-31；S001/S002 选型；R001 数据模式 |
| D02 | WSL Linux 下接管 ome 开发（构建、测试、平台门控） | 已交付 | [推断] 追溯登记（2026-09-05） | GOAL 历史 2026-08-31；R010（已归档） |
| D03 | 扩展 ome 支持 Linux 本机软件部署（agent 除外，归 ohmyagents/ohmypwsh） | 已交付 | [推断] 追溯登记（2026-09-05） | GOAL 历史 2026-08-31；平台抽象层与 linux 字段族 |
| D04 | ohmypwsh 与 ome 的 Linux/Windows 现状对齐，再推进 mac 接管 | 已拒绝 | [推断] 追溯登记（2026-09-05） | 原因：2026-09-01 被完整迁移裁决取代（superseded）；对齐待办关闭，数据迁移改走 P0026 M0 单向回流；R012 降级为数据迁移参考。防复问：不再回到「双向对齐」路线 |
| D05 | 承接 ohmypwsh 部署、验收、自愈完整迁移（M0..M6 里程碑，验收口径原 ohmypwsh P0026） | 已交付 | 2026-09-01 用户裁决（完整迁移取代共存对齐）；2026-09-07 D18：源项目不存在，M6 取消 | M0..M4 已收盘；M6 随 D18 取消 |
| D06 | 用 project-evo 重建文档体系：补 PRD/proven/ROADMAP/AGENTS 义务表，INDEX 对账磁盘修断链断号，diary 禁字清剿（PE-12 全绿） | 已交付 | 2026-09-05 用户两裁：全面重整档 + PRD 全量追溯 | evo check 13 项全绿（PE-06 合法 SKIP）；GOAL 时间线 2026-09-05；diary 同日 |
| D07 | 命令原语重构第一批：doctor 升 ome 核心命令做三层诊断（系统 / agent 二进制加版本加 token 可用性 / 依赖六类加环境错误检测），检测驱动安装，agent 二进制入册 ome catalog 由 install/deploy 幂等承载；oma 收窄为配置 agent、hook、编排（D06 方向反转） | 已交付 | 第 3 轮共六裁（2026-09-05）：ome 承载、oma agents install 迁册 ome、六类（agent / 运行时 / 运行时管理器 uv 加 fnm / 编译器含 vsbuild / cli 工具 / 运行时衍生）、先 doctor 命令面大重构另立项、oma 登录态等四类检查归 agents 域、doctor 不取设置 | 四切片全收：九类 taxonomy 与入册（461a178）、doctor 三层（9179893）、全链适配与 D08 渠道（8c4953e、9c48903）、oma 迁册由 oma 侧 P0029 交付；双端验收绿 |
| D08 | 全软件分发兜底渠道：download 层官方渠道失败回落 env.ohmygh.com 镜像（按 `<tool>/<version>/<asset>` 模板拼 URL，sha 校验同 catalog pin 值），为默认分发切换自建做准备（用户方向：Cloudflare Worker 加 S3 加自有域名自己管理分发）；D07 切片 3 的渠道兜底部分（grok GCS、kimi CDN manifest）并入本需求统一实现；第二批（ohmyenv-rs#7）：evergreen 引导器（rust / vsbuild）官方失败回落镜像 latest 段，`.sha256` 边车即信任锚（先边车后资产，取不到拒绝无校验下载） | 已交付 | 第 1 轮三裁（2026-09-05，随 ohmycloud D36 同轮：域名 env. 子域、全量一步到位、先手动种子后 Actions 自动）；第二批 2026-09-08 随镜像 v2.4 补种收口 | download_asset_with_mirror 落码双端断源实测绿（8c4953e）；ohmycloud 种子 69/69；darwin 补种 ohmycloud#3 进行中；第二批 download_latest_with_sidecar 落码 `OME_TEST_MIRROR` 6/6 绿（zoxide linux 回归、ffmpeg HEAD 在位；selfupdate 边车权威统一至 download 层；ome 自身 dev 通道断源实测待镜像补 `ome/dev` 段） |
| D09 | 命令面 agent 友好化（参考 evo agent-native CLI 契约，用户定调 2026-09-07「所有命令参考 evo 的 agent 友好实践简化，命令主要给 agent 使用」）：发现层（根 SKILL.md 与 `ome --llms` 紧凑清单）、CTA（诊断缺口带下一步建议命令）、输出契约对齐（kv 紧凑已达、R013 在档）；另含自适应 `ome skill`；命令面合并或收敛（增减裁决）另批 | 已交付 | 第 0 轮（用户方向指令） | 74f8752 三件；dd2ecdd `ome skill`；CLI 冒烟 `--llms` 与缺子命令 |
| D10 | ome 功能原语定义（2026-09-07 用户裁定：三原语加派生）：**三原语**为 doctor（检测诊断：三层加环境错误）、install（幂等安装：下载加 PATH、注册表与配置，检测驱动，本地与目标一致即免装）、status（三态对照：锁定/已装/PATH）；**派生面**语义挂靠原语不另立：query 为解析前置（install 的 dry 态）、update 为 install 时变、pin 为锚操作（数据面）、verify 与 heal 为断言与自愈组合、init 与 self 为辅助通道。`deploy` 已去掉并入 install（D15）；`daily` 已去掉，升级走 update（D16）；`package` 已去掉（D17）。后续命令面重构与 D09 实现以此为准 | 已采纳 | 第 1 轮（三原语加派生）；第 2 轮 D15 去掉 deploy；第 3 轮 D16 去掉 daily；第 4 轮 D17 去掉 package | SKILL.md 与 R013 与 AGENTS 意图路由同步原语口径 |
| D11 | doctor 配置健康面扩展（用户 2026-09-07「诊断不仅要检测 PATH 变量、SHELL 使用命令情况，还要检测运行时配置、编译器配置和依赖等所有配置和使用正常不」）：在 PATH 卫生与命令探测之外加**配置健康节**：运行时配置（npm 源、bunfig 遥测、cargo sparse 镜像、GOPROXY 等 heal-mirror 域判据）、编译器配置（rust toolchain、go env 关键变量）、依赖与密钥载体（heal-keys 域）逐项 OK/WARN/FAIL；检测判据与 heal 自愈动作同源（检测-自愈闭环，heal-map 数据源复用）；密钥域不管（用户裁定），关注面收敛为软件安装部署与默认配置（用户裁定） | 已交付 | 第 0 轮（用户方向指令） | d4c96bb 五项真机绿（goproxy 抓真缺口）；README/INDEX 同步 |
| D12 | 特型工具部署深诊（用户 2026-09-07「vsbuild 和 rust 这类应该可以深入诊断安装部署情况和部署后的状态」）：evergreen/安装器型工具的**部署产物核对**：rust（rustup toolchain stable 激活态、rustc/cargo 双在）、vsbuild（组件三件套实装：MSBuild.exe 与 VC\Tools\MSVC 在安装目录、机器级 PATH 注册态）；判据从 install/vsbuild.rs/rustup.rs 的写入动作与布局字段来（写入动作背书纪律的部署产物延伸） | 已交付 | 第 0 轮（用户方向指令） | d1777b2；rust 探测改走 EnvRoot rustup 加 RUSTUP_HOME |
| D13 | doctor 网络通连诊断（用户 2026-09-07）：下载分发与官方渠道 HEAD 探测（GitHub API/资产、镜像、aka.ms、rsproxy、goproxy、npmmirror 等），短超时并行，不通走 WARN 不拦退出 | 已交付 | 第 0 轮（用户方向指令） | d20bed3；总超时 5s；网络 WARN 不单独把 verdict 打成 degraded |
| D14 | 入册 ffmpeg：官方仓 FFmpeg/FFmpeg 只提供源码，预编译按 ffmpeg.org/download.html（Windows gyan.dev / GyanD/codexffmpeg essentials，Linux BtbN/FFmpeg-Builds n-release GPL 静态，macOS evermeet.cx 仅 Intel、ome mac 为 ARM 故空态） | 已交付 | 第 1 轮（用户给 GitHub 仓与官方下载页） | Windows 真机 install 9.0.1 幂等、path=true；linux_sha256 已回填 |
| D15 | 去掉 deploy 子命令：install 一次完成下载、PATH、注册表与配置；update 为 install 到最新并锁定 | 已交付 | 第 2 轮（用户 2026-09-07 终裁，取代「更名为 download」） | CLI 去掉 deploy/download 子命令；--llms 与 cli 25 绿 |
| D16 | 去掉 daily：同主/跨主分流语义过重，升级一律 `ome update` | 已交付 | 第 1 轮（用户 2026-09-07） | CLI 删除 daily；库测 87、cli 25 绿 |
| D17 | 去掉 package 子命令：打包分发不再由 ome 承载 | 已交付 | 第 1 轮（用户 2026-09-07） | 删除 CLI 与 src/package.rs；cli 28 绿 |
| D18 | 独立仓：不再有 ohmypwsh 项目；ome 为独立仓库，成功标准为命令在部署系统上功能完整（doctor/status/install/query/update/pin/init/verify/heal/skill/self update/--llms） | 已交付 | 第 1 轮（用户 2026-09-07：不再有 ohmypwsh，只管本机命令面完整） | 本机 doctor/status/query/pin/verify/heal --dry-run/install ffmpeg/skill/--llms 可跑；cli 28、clippy、四件套绿 |
| D19 | 生态兄弟仓：ohmycloud 是资源分发基建（env.ohmygh.com）；ome 官方渠道失败回落该镜像（D08），不自建分发面 | 已交付 | 第 1 轮（用户 2026-09-07：ohmycloud 是分发基建兄弟项目） | AGENTS/README/ROADMAP/SKILL 定位同步 |
| D20 | 种子资源清单用 GitHub ISSUE 向 ohmycloud 派任务并对齐；catalog 为唯一权威，脚本抽可入镜对象 | 已交付 | 第 1 轮（用户 2026-09-07：种子清单用 ISSUE 和 ohmycloud 派任务和对齐） | R014；ohmycloud#5；ohmyenv-rs#8 |
| D21 | 向 ohmycloud 发 ISSUE：对齐 ome 自身与其管理软件的分发、更新、安装三面 | 已交付 | 第 1 轮（用户 2026-09-07：给 ohmycloud 发 ISSUE 如何对齐分发、更新、安装 ome 和 ome 管理的软件） | ohmycloud#8；姊妹 ohmycloud#6 |
| D22 | rclone 入册（ohmycloud#9 请求：env-seed 上传链切 R2 S3 直传提效）：catalog 三平台节（SHA256SUMS 统一清单锚，win zip 展平、linux/mac zip-bin 单二进制）、toolver 探测正则、真机安装 pin 回填；种子差集随派 | 已交付 | 第 0 轮（用户 2026-09-08「继续」推进队列，镜像方 ISSUE 请求） | win 1.75.1 真机 install 绿（sha 与官方 SHA256SUMS 逐字一致）；linux/mac pin 待对应机器升级回填 |
| D23 | browser-harness 与 reader 重入册（撤 2026-09-07 暂不接管裁）：reader 节恢复升 v0.6.0 三平台（.sha256 后缀边车锚）；browser-harness 仓已重写 TS（browser-harness-ts，npm 包 tgz），新增 **npm-tgz 安装通道**（tgz 过锚下载后 npm install -g，bin bh 进 npm 全局 bin、PATH 现查探测与三态、cmd shim 经 cmd /c 拉起）；需 node 与 npm 在 PATH（fnm 供给） | 已交付 | 第 0 轮（用户 2026-09-08 指令「增加 browser-harness 与 reader_rs」） | 两工具 win 真机 install 绿、status 三态全齐（browser-harness locked=installed=0.5.1 path=true；reader 0.6.0）；测试全绿；计数 42 改 44 |
| D24 | lightpanda 入册（无头浏览器：CDP serve / fetch / MCP server / AI agent）：上游无 Windows 构建（win 空态），linux/mac 裸二进制 copy 落 ~/.local/bin；官方无 sums，pin sha 取 GitHub digest；`releases/latest` 被 nightly 滚动 tag 占据，升级须显式 `--tag`；探测走 `version` 子命令（`--version` 是 UnknownCommand） | 已交付 | 第 0 轮（用户 2026-09-08 指令「增加 lightpanda-io/browser」并裁「先本机安装」） | WSL 本机先行安装绿（sha 与 GitHub digest 逐字一致，`lightpanda version` 实测 0.4.0）；catalog linux/mac pin 0.4.0；计数 44 改 45 |
| D25 | `ome skill` 逐工具自适应引导：catalog 增 `desc`/`guide_env`/`guide_dirs`/`guide_notes` 四字段（45 工具全量起稿），渲染面输出每工具「三态标记 + 用途 + exe 实测 + env 键实测（凭据只显在否）+ 目录实测（在位与否）+ 注意事项」；顺修 cmd_skill 落盘被静态版覆盖的既有 bug（write_skill 落自适应文本，静态骨架仅 init 兜底） | 已交付 | 第 0 轮（用户 2026-09-08 需求「查询本机已装工具与运行时、引导 Agent 用途与注意事项（env/path/版本）」；AskUserQuestion 两裁：扩展 ome skill 面、全 45 工具起稿；补充裁「结合安装路径、环境变量、数据目录自适应生成」） | 本机渲染实证（rust 块 env 四键实测值、GH_TOKEN 只显已设、空态如实标）；门禁全绿 |
| D26 | gitleaks 入册（密钥泄漏扫描，密钥安全管理类第三员）：checksums.txt 统一清单锚（sums_asset 带版本占位）；三平台单二进制（win zip 展平、linux/mac targz-bin）；toolver 加探测正则 | 已交付 | 第 0 轮（用户 2026-09-08 指令「增加 gitleaks 安装」） | win 8.30.1 真机 install 绿（sha 回填与清单及 digest 三方逐字一致）；linux/mac pin 清单直填；计数 45 改 46 |
| D27 | 种子上传自维护（镜像方 D41 对偶件，用户裁双仓自维护 S3 种子）：路线 A build.yml mirror job（dev 产物灌 ome/dev 加 ome/latest 沙滚段）；路线 B seed-mirror.yml（每日加 catalog push 触发，catalog 软件集域面 diff 后 rclone 直传）；`.tools/seed.py` uv 单件（无 sha 不入镜、pending 队列、边车自算即锚、NO_CHECK_BUCKET 必带）；ohmycloud env-seed 退居灾备对账 | 已交付 | 第 0 轮（用户 2026-09-08 裁 D41 双路线加 uv Python 形态补充） | 本机 `--plan` 域面 diff 实测（98 对象 93 synced，精准抓 gitleaks 缺种与 lightpanda mac 错配）；首次 push 后 CI 双 job 域面自动到货即验收 |
| D28 | 入册清单化（toolver 正则漏带两犯加 digest 错配两族坑的统一机检，2026-09-08 diary 候选升格）：已装版本探测参数与正则自源码硬编码表迁 catalog 字段（`probe_args` 缺省 `--version`、`probe_pattern`，避开已占用的 `version_pattern` 资产提版语义）；catalog 结构机检测试（在管必有探测正则、正则可编译含捕获组、sha 字段 64 hex）；R001 增入册 checklist 节（含 M014 正解：pin 落库后跑 `seed.py --plan` 域面 diff 替代人眼） | 已交付 | 第 0 轮（用户 2026-09-09「继续」推进 diary 候选） | 46 节回填探测行为不变（真机抽查八工具全对）；R001 字段表与 checklist 节；tests/catalog_lint.rs 两测绿 |
| D29 | 三仓对齐共识（ohmycloud 提案 2026-09-10，oma 侧同日对线确认）：omc agent deploy 全面委托 ome（env 镜像拉 ome 二进制加 catalog 推端加 `ome install claude codex sops age`，豁免端仅装工具面）；omc 顶层新增 ome/oma 透传。ome 侧确认三点：sops/age 三平台槽位持续维护（sops 3.13.3、age 1.3.1）、claude linux/mac 资产链随 ohmyenv-rs#10 推进（缺口 1 pattern 已修）、ome release 后发 ISSUE 知会 ohmycloud 同步 omc tool status 镜像锚。补充裁（同日）：ome 与 omc/oma 集成功能优先级为诊断检测、恢复治愈、安装部署配置之序；oma CI 直推 S3 入镜与 ome 镜像节奏对齐 | 已交付 | 第 0 轮（用户携 ohmycloud 共识提案与补充裁，2026-09-10） | R014 六（共识唯一权威）；AGENTS 义务表发布行与边界；ROADMAP 集成优先级与阶段六；回执经 herdr 会话同步；#10 缺口 1 修复（M014 增补、catalog_lint pattern 机检） |
| D30 | 三仓版本里程碑对齐与重叠削减（用户裁 2026-09-10：omc/ome/oma 各发新版本对齐；omc agent 功能与 ome 不许冲突，重叠由 ome 侧减）。定稿（omc 回执 2026-09-10）并当日封版（用户急令提前，原拟 09-12）：三仓同水位 omc 0.3.0（已发）/ ome v0.2.0（首个 tag，Cargo 0.1.0 从未封版，Unreleased 全量归 0.2.0）/ oma v0.5.0。削减清单：doctor agent 层五字段整层移除（装态对账归 omc、token 检测归 oma diagnose），收窄为系统加依赖两层（依赖层智能体依赖组保留：install 域单机装态事实与 omc 对账数据源；D10 三层口径随改）；ome 自身首次装位归 omc tool 与 ome init 双通道同装位，升级单通道 ome self update，在管工具 install/update 全归 ome、omc 透传；接口承诺 omc agent deploy 调 ome install 契约冻结（子命令/参数/退出码/R013 冻结面），削减只动 doctor 自身输出面。另定协调项：yolo 托管端用户级以 omc patchCodexConfig 为准（oma 面向项目级）、pretrust 归 oma init（ome 无对应面，登记口径） | 已交付 | 第 1 轮（用户裁方向并征三提案）；第 2 轮 omc 回执（2026-09-10：削减清单无异议、推送确认、tag 后 ome/latest 拆 stable）；第 3 轮用户急令（2026-09-10 晚：当日封版不等 09-12，ohmycloud v0.3.0 已发）；重叠面已对 omc 源码实证（doctorEndpoint 舰队装态自检、tool.ts Q53 只管装不管升） | 削减落码（a9b069d）；v0.2.0 当日封版发布（feac7d7 加 tag v0.2.0，CI 三平台正式 release，self update --stable 验收部署位 0.2.0、doctor degraded 无 FAIL） |
| D31 | 小版本完美状态同发（用户裁 2026-09-10：#10 余量清零达成干净 main 后发 v0.2.1，herdr 知会，omc 侧锚同步；omc v0.3.1 已发并消费 ome/stable）：一（codex 布局）linux/mac 装入 `~/.local/share/codex` 并注册其 `bin` 到 PATH（tar 内 bin/ 嵌套与 codex-path/resources 多载荷保留，与 win 同构；原直解 ~/.local/bin 致装后验证必败，ohmyenv-rs#10 缺口 2）；二（catalog 自举）裸二进制端四级候选全 miss 时自动拉取落位用户数据目录：官方 raw（main 分支）优先、镜像 `ome/catalog/tools.toml` 边车锚回落（先边车后资产，解析验证防半截；seed.py 路线 B 增量推 catalog 本体加边车，#10 缺口 3） | 已交付 | 第 0 轮（用户裁，2026-09-10） | codex catalog 字段修复加 bootstrap_catalog 落码（测试全绿）；v0.2.1 tag 封版（CI v* 自动直推 ome/stable 首跑）加 self update --stable 验收 |
| D32 | typst 入册（文档排版系统 CLI：compile / watch / init 子命令出 PDF，公式与文献排版）：三平台单二进制（win zip 展平，linux/mac tarxz-bin 按叶子名取 typst 落 `~/.local/bin`；mac 取 aarch64 资产，上游另有 x86_64-apple-darwin）；官方 release 无统一校验清单，pin sha 取 GitHub digest（与 lightpanda 同锚形态），三平台资产经本机下载实测哈希逐字核验；探测走 `typst --version` 正则 | 已交付 | 第 0 轮（用户 2026-09-10 指令「增加 typst v0.15.1 的安装」，点名 release tag） | win 真机 install 幂等二连绿（sha 与 digest 逐字一致）、status 三态齐（0.15.1 加 path=true）；catalog_lint 与黄金文件未破；计数 46 改 47（AGENTS/README/SKILL/INDEX/catalog 头注释）；推送 main 后 seed-mirror 路线 B 自动补种（uploaded 4），镜像三资产与边车 HEAD 200 且 sha 与 pin 逐字一致；diary 2026-09-10 |
| D33 | 软件清单云端化与实时刷新（用户方向 2026-09-10：「我们的软件清单应该放到 env 云端可以实时更新，这样不需要动 ome 就可以随时通过配置播种新软件」）：运行态 catalog 由云端（env.ohmygh.com `ome/catalog/tools.toml` 加 `.sha256` 边车锚）刷新，消费侧不依赖 ome 发版即得新工具与新 pin；结构定稿（第 3 轮用户裁「catalogsync 与 catalog 应是一个主功能下面的 2 个子功能」）：主功能只留一个入口 `ome catalog`，status（看）与 sync（刷）是其两子功能 | 已交付 | 第 1 轮（用户方向）；第 2 轮（用户令「开工」按建议三项定稿）：权威落位取「仓库副本是开发与离线兜底源，云端是运行态权威」；刷新时机取「TTL 24h 自动（`OME_CATALOG_TTL` 秒级可调、`OME_OFFLINE=1` 关）加显式子功能 `ome catalog sync`」；信任锚取「沿用镜像自算 sha256 边车（先边车后资产），签名留后」；第 3 轮结构裁（2026-09-10）：独立 `catalogsync` 概念取消，刷新逻辑并入 catalog 模块成为 sync 子功能，状态采集为 status 子功能 | 主功能落码（`src/catalog.rs` 内 status 采集与 sync 刷新两子功能，单测五枚加 gated 真网测）；真机 `sync` 把部署副本刷到云端 CFF2B44A，旧部署二进制 ome 0.2.1 在非仓库目录读到 typst 三态齐（实证配置播种不必换二进制）；`OME_CATALOG_TTL=0` 不刷新与人为漂移自动还原双实证；R013/README/SKILL/`--llms`/AGENTS/R001/CHANGELOG 同步，diary 2026-09-10；schema 边界在档（新 extract 类型或新逻辑仍须改代码） |
| D34 | 云端清单防 MITM 的非对称校验（用户 2026-09-10：「云端这个 catalog 文件应该要想办法校验防 MITM」「本地和云端一起非对称校验，像 SOPS 的方式」）：用非对称密钥对运行态清单做真实性校验，云端拉取与本地部署副本两侧都验；密钥分工与保管参照 SOPS 习惯（公钥标识进配置、私钥只在本地与 CI 密钥库） | 已交付 | 第 1 轮（用户方向，研究落 S006）；第 2 轮（用户令「开工」，取建议默认）：密钥载体取新建 Ed25519 签名密钥对（minisign 格式，私钥本机 `~/.config/ome/catalog-signing.key` 加 GitHub Secret，公钥进仓库并内嵌二进制）；本地校验强度取「有签名但验不过即阻断，缺签名件只告警」（本地 pin 回写会撤签名，避免误伤正常回写）；候选 B 的清单内单调序号（防回滚）留作下一步 | 落码：`src/catalog.rs` 内嵌公钥与 `verify_with_embedded_keys` 加 `check_signature` 加签名件随刷新落位加 pin 回写撤签名；`.tools/catalog-sign` 签名工具（keygen 与 pubkey 与 sign 与 verify）；seed-mirror 加签名步与 `seed.py` 上传 `.minisig`；单测三枚加 gated 真网测增补；`catalog status` 加 `signature` 与 `pubkey`；推送后 seed-mirror 手工 dispatch 首发签名件（镜像 200），本机 sync 验签通过落位、status 报 signature=valid，篡改联网自动修复与离线阻断加 sync 自愈双实证；R001/R013/README/SKILL/AGENTS/CHANGELOG 同步 |
| D35 | 软件清单发布、更新与播种流程成标准（用户 2026-09-10「现成要形成一套标准」）：三流程唯一流程权威落 R015：发布（仓到云：checklist 加 CI 签名缺密钥拒发加三件套幂等重灌）、更新（云到端：sync 与 TTL 与自举三通道、三重门、加载前巡检、升级 runbook）、播种（路线 A 自产段与路线 B catalog 域面 diff 与无 sha 不入镜）；字段语义 R001、跨仓 R014、签名研究 S006、测试闸门 R004 互指 | 已交付 | 第 0 轮（用户令成文） | R015 定档；INDEX 编号表与 AGENTS 方案索引挂链 |
| D36 | 管辖边界两域分治（用户 2026-09-10 两裁，先误读「停发版」后当场更正）：ome 保留自身代码与功能更新、发版（tag 与 CI 与 ome/dev 加 ome/stable 自产段）、二进制自更新（self update 三通道）；ome 所管软件的**清单发布更新运营**与**资源播种分发运营**托管 omc（catalog 数据权威仍在 ome 仓唯一 pin 源；承接形态由 omc 定：消费现供给零迁移或接管签名播种流水移转 CATALOG_SIGNING_KEY；承接期间本仓流水不撤保供给连续） | 已采纳 | 第 1 轮更正（用户中断澄清「代码功能更新和二进制自更新还是自己维护，只是清单和下载资源托管 omc」） | R015 五管辖边界节；AGENTS 边界半句；R014 六第 7 条；D37 修订：数据权威与发布门亦移交 omc（数据权威留 ome 口径作废） |
| D37 | 管辖边界终版（用户 2026-09-10 再裁「ome 和 catalog 数据权威加 catalog_lint 发布门完全解耦，omc 管所有软件清单数据，ome 只管诊断逻辑和安装部署逻辑」）：权威 tools.toml 与 catalog_lint 发布门迁 omc（批 3）；ome 留诊断逻辑、安装部署逻辑、清单格式契约（R001）、自身发版与自更新；ome 仓权威 catalog 退役为测试夹具，端上清单云端拉取；待对线定案：权威 toml 落位与契约遵循、lint 门移植与消费面演进同步机制、pin 回写与云端权威的覆盖语义、入册流程改道 omc 数据面 | 已采纳 | 第 1 轮（用户澄清更正 D36 半句口径） | R015 五终版表与二节改写；R014 五/六.7；批 3 撤退已执行（权威件退役、seed-mirror 整撤、update 收窄与 drift CTA 落码）；omc 权威面首发验签全绿 |
| D38 | 消费面镜像直装（omc 打包封板收尾请求：omc 以 npm-tgz 形态入册后，lan-mac 实测 ome install omc 卡在私有仓匿名查 GitHub API 404）：pin 四键齐（锚 = pin sha256）且 GitHub API 失败（私有仓匿名 404、限流、断网）时，解析段直拼 env.ohmygh.com 资产域 URL 过 pin 锚安装；`OME_MIRROR=1` 扩展为解析面强制镜像（与 selfupdate 同名开关同源）；无锚不回落红线不变；R013 契约零变更（url 如实呈现镜像地址） | 已交付 | 第 0 轮（omc herdr 提案，2026-09-11；评估成立：pin 驱动解析本不需 API，私有生态对症） | resolve.rs 回落与强制双路径；gated 真网测 8/8；真机双路径实证（omc v0.3.2）；v0.4.0 封版 |
| D39 | 安装配置部署逻辑数据化（用户方向 2026-09-11：参考 winget/scoop 运营，清单 DSL 或脚本承载各软件安装配置部署方法，数据化进云端配置管理，ome 简化为只执行清单元数据）：S007 实证两家形态（winget 纯声明 DSL、scoop 任意 PS 脚本），提案三层：L1 声明原语（env_set/shims/persist/machine_path/elevate）、L2 受控命令数组（跨平台分键 post_install）、L3 任意脚本不进（例外走引擎内建原语由数据触发）；DSL 与 tools.toml 同件同签名（D34 链零新增信任面）；目标是新工具与配置变更零改 ome 代码、数据面单方可配、专用模块（vsbuild/rustup/docker/msi）拆解为原语组合 | 已采纳 | 第 0 轮（用户方向指令）；S007 研究在档（含五节全平台约束与跨平台先例）；全平台为第一设计约束（用户 2026-09-11）：原语优先（三平台语义引擎保证、新原语过三平台矩阵才准入）、L2 三键齐备 lint、平台不适用容忍复用 M003、平台矩阵即验收 | 交付物升格（用户令 2026-09-11「要形成 ome 云端的 catalog 和 manifest 标准」）：**R016 云端清单与 manifest 标准**：A 层 catalog schema（资产解析/布局/探测/pin，吸收 R001 字段语义）、B 层 installer manifest DSL（三层提案原语规范）、元属性（schema 版本化字段、跨仓执行：omc vitest lint 照标准机检、D34 签名链覆盖、全平台四原则）；omc 评审三待定点全裁 CONFIRM（persist reserved、L2 尾行报告、单件定案），R016 v0.2 正式版定稿；引擎首波落码（manifest.rs 解析与 L1/L2、install 双轨接线、catalog sync 扩拉三件套、lint 扩面与 fixtures），三轮补审（codex 审 fc196b8..2b75a86 十一件）修 L2 管道抽干假超时（M017）、.cmd 兜底相对定位、manifest 拉取吞错与同锚不刷、shims 原语判定与落点、manifest 节键解析，测试 8 加 lint 4 全绿；待共识：manifest 仅显式 sync 刷新、撤内建前的新鲜度门与 parity 门；待 omc 一周内扩 catalog-seed 两件三件套与 DSL lint |
| D40 | 回滚重放防护引擎侧（omc 数据面先行落地后的 ome 半面）：tools 与 manifest 各自独立 seq 门：拉取链读顶层 seq（缺省 0），低于已见拒收（报错不降级，提示手动删 seq 记录出路）、等于幂等重放、大于收并记 `.<名>.seq` 同目录存储；generated_at 为信息性非安全边界；omc 侧签发坑知会收讫（读现值直取 R2 本体防代理层旧读，ome 引擎仅消费无需读现值） | 已交付 | 第 0 轮（omc 契约知会 2026-09-11） | seq_gate 与 toplevel_seq 落码；WSL 真机实证（拉通记 2、伪降拒收）；双端全测绿 |
| D41 | 更名 Ark（用户 2026-09-11 定夺「ome 正式更名 Ark（Agent Runtime Kit）」；2026-09-12 补裁：GitHub 仓改 raystyle/ark-rs、本地目录迁 `D:\ark-rs`、首发版 1.0.0）：产品与命令与仓更名；环境变量与元数据目录切 ark 主名并读回旧名兼容；EnvRoot 物理目录与泊位语义不动；镜像段 `ark/` 兼容读 `ome/`；catalog、manifest、seq 门禁整体带入 1.0.0 | 已交付 | 第 1 轮用户定夺（2026-09-11 七点口径）与补充决议（2026-09-12） | v1.0.0 发版（tag CI 绿、六资产双名、omc 三件批切回执）；GOAL 历史 2026-09-12；过渡期运营见 TODO |
| D42 | 运行时源中国镜像统一落 manifest（用户 2026-09-13 重投裁定 + 补充裁定）：manifest DSL 扩 mirror 节，post_install 按节落源、不散各端脚本（fnm 面 FNM_NODE_DIST_MIRROR=npmmirror node dist 加 npm registry=npmmirror；uv 面 ~/.config/uv/uv.toml 清华 TUNA 加 pip.conf 加 UV_PYTHON_INSTALL_MIRROR=NJU；bun 面 bunfig=npmmirror；win 用户环境变量 / POSIX shell rc）；rust 接管扩 POSIX（装走 rsproxy.cn rustup-init、RUSTUP_DIST_SERVER/RUSTUP_UPDATE_ROOT=rsproxy、~/.cargo/config.toml rsproxy 全量形态）；跨平台幂等、存量端升级即得；全集对齐 ohmypwsh set-mirror.ps1/P0017/P0019 遗产口径，omc bootstrap 同口径兜底在 ark 到位后撤；wsl 总台 verify A13 四件断言为验收面 | 已交付 | 第 1 轮（用户重投 + 补充裁定 2026-09-13） | v1.1.0 发版（tag CI 绿）；WSL 验收 A13 六件 PASS（对岸撤兜底后仍绿）；R016 mirror 节；对线三轮 CONFIRM 入 diary；遗留（镜像桶 bun 资产、总台环境红项）已回执对岸 |
| D43 | zig 版本去锁（用户 2026-09-13 裁定「不再锁定 zig 版本」）：zig 条目去 pin 转 latest 滚动；引擎侧三件：resolve 分支 a 泛化吃 ziglang index.json 顶层版本键形态（per-target tarball/shasum，官方 sha 直值进 Resolution 新字段 official_sha256）、布局字段（dir/bin/exe）支持 {version} 占位（探测 glob 候选取 semver 最大，M025 同型；安装期按 res.version 直替换）、expected_sha256 官方链前置插入直值锚（D08 回落门天然满足）；数据面配合项知会 omc（去 pin 四元组、挂 cdn_index_url、cdn_asset_pattern 改 target 键形、exe/bin 占位化、cdn_url 模板退役） | 已交付 | 第 0 轮（用户裁定 2026-09-13） | v1.2.0 发版；对线两轮 CONFIRM（Z1 glob 双分隔符实锤修复）；WSL 端到端绿（latest 解析、镜像优先锚校验、占位布局、幂等/update、zig version 跑通） |
| D44 | 下载链反转镜像优先（用户 2026-09-13 裁定「ark 安装默认走 ohmygh，其他官方渠道是兜底」，令发 1.1.1）：download 层两条链（主资产与 evergreen 边车）反转为 env.ohmygh.com 镜像单次快速首试、失败回落官方完整链（retried 加 curl）；校验锚语义不变（pin/边车/digest 照常，锚不符视同镜像失败回落）；「有锚才回落」红线改为「镜像为主、官方兜底、有锚必校验」；覆盖 install/npm-tgz/rustup/vsbuild/selfupdate 全调用面 | 已交付 | 第 0 轮（用户裁定 + 发版令 2026-09-13） | v1.1.1 发版（CI 绿、stable 段 200、WSL 到位）；对线两轮 F1-F7 加 G1-G4 全修 CONFIRM；真机实证「已下载（镜像优先）」；go goproxy 键认领生效 |
| D51 | 镜像优先转正默认通道与 GitHub 兜底（用户 2026-09-18 两连发「github 应该是兜底，不是默认流程」「各仓的自更新应该也是默认 ohmygh.com」，详篇 ADR-0008，验收 REQ-0010）：三面翻转：解析面 pin 驱动零 GitHub API（镜像直装 URL 主通道加 Resolution.fallback_url 官方确定性直链兜底，D38 API 失败回落路径收编删除；pin 三键缺一或显式 latest/tag/version 才打 API）；update 解析改 pin 驱动对齐 catalog pin（云端最新定义改指镜像与 catalog，D49 三态不动）；selfupdate 元数据镜像段读序先行官方 API 兜底；ARK_MIRROR=1 转正默认兼容 no-op、ARK_MIRROR=0 官方优先逃逸阀（读回 OME_MIRROR） | 已交付 | 第 0 轮（总台派单转呈用户两连发裁定） | cargo test 全绿；离线集成测 query 零 API 走镜像；待五端舰队实弹复跑回执 |
| D45 | sync 的 tools seq 消费收口（omc 回执 2026-09-13 认领请求，CF 三犯根治端上面）：current 判定收紧为 sha 同锚且云端 seq 不高于已见（锚探测与 toml 拉取异源，CF 残影下边车旧本体新时原判定丢新档） | 已交付 | 第 0 轮（omc 认领请求 2026-09-13） | v1.2.1；is_current 纯函数入测 |
| D50 | skill 面撤除与 `--llms` 唯一发现通道（详篇 ADR-0007，验收 REQ-0005）：`ark skill` 与 SKILL.md 与自适应渲染退役，`--llms` 头部吸收何时用与下载纪律行；guide 四字段转数据面保留 | 已交付 | 第 0 轮（用户裁定 2026-09-16） | cargo test --release --locked 全绿；五端旧 SKILL.md 清扫（lan-win 双位与 lan-ubuntu 与 lan-linux 各清一份，WSL 与 lan-mac 本无）；门禁全绿 |
| D49 | update 漂移收口（ohmycloud 舰队分型对线 2026-09-15：宿主十件全是 installed 落后 pin 的 a 形态，resolve==pin 的 skip 挡住真落后装机）：update 的 skip 判据从「云端无新版」扩为三态（一致 skip、落后或未装真装 pin 版、领先如实报并指数据面滚锁）；HINT 文案「落后锁定」改「与锁定不一致」（判据双向不等）；pin_drift 纯函数入测（未装与解析失败保守按落后）；尾修 bug4：cat.select 收逗号串（HINT 文案 join 逗号复制即跑，update/install/query/pin/heal 全调用方统一，保序去重滤空段）；尾二修（舰队验收回执）：status drift 判据统一 pin_drift 数值口径（git 的 .windows.N 后缀形态数值等、旧字符串比较恒列的口径差），npm-tgz 类不进 HINT（版本真源 npm registry 端上自管，omc 恒挂形态），status_drift_hint 纯函数入测 | 已交付 | 第 0 轮（ohmycloud Q2 提案批准附 HINT 文案顺带） | 本机 hst 实测 Ahead 语义路径；宿主 pwsh（MSI 型 extract=msi）与 wsl 待舰队实测补装 |
| D48 | oxvg fork 出 CI 分发链（用户加单 2026-09-15「我们只要 cli」）：fork 仓 raystyle/oxvg 三岗矩阵（ubuntu linux gnu、ubuntu mingw 交叉 win-gnu 摆脱 VC、macos arm64），资产 oxvg-版本-triple 对齐 ohmycloud catalog pin 名加 sha256sum 边车，main 滚 dev prerelease 与 v* tag 正式双通道，零 R2 secrets（镜像归 ohmycloud env-seed 从 release 拉）；上游六工作流全撤（wasm/jsx/napi/size/benchmark/checks 含 node 面），CI 面收敛 CLI 一条线，packages 与 node 工程文件保留保上游 merge 面 | 已交付 | 第 0 轮（用户派单六点）；第 1 轮 codex 快核（G1 CRLF 版本提取实锤加 G2 release create 竞态已修） | v0.0.7 正式发布（三岗绿、tag 重指 fork HEAD、六资产到货）；catalog 已滚（mac 字段补齐、镜像换血、mac digest 以 release 终值 427c4415 为准（双 run clobber 后取静值））；舰队五端全齐，双侧闭环 |
| D47 | OfficeCLI 升格自维护 fork（用户裁 2026-09-15：接管编译维护加自更新加自维护分发，与 ark 集成无缝）：fork 仓 raystyle/OfficeCLI（基线 5a938d2=upstream/main 头）；修上游 v1.0.150 win-x64 启动即崩（System.IO.Pipelines 传递依赖被 trim 裁出 bundle，显式引用保闭包加 CI 同步 `__update-check__` smoke）；自更新源切自家（env.ohmygh.com/officecli 镜像主加 fork GitHub 兜底，原地换语义保留）；fork 版本序 1.0.151 起跳；CI 出 9 资产同形（8 RID 加 SHA256SUMS）；ark 集成待 fork release 后 ohmycloud 滚 catalog | 已交付 | 第 0 轮（用户派单四件 2026-09-15）；第 1-2 轮 codex 对线（一轮撤无效 Pipelines 引用改判截断文件类加 selftest 冒烟加 trim 闭包断言；二轮 musl 非静态撤真跑加 installer 离线短路）；G3 裁定（特形段缓议、读序不动、镜像只播 fork） | **v1.0.151 已正式发布**（九资产同形加 SHA256SUMS，CI 九岗绿含 release，tag push 不触发怪癖走 dispatch --ref 通道）；mac 签名步 HAS_CERT 门（fork 无证书跳过）；本机 WSL 已升 1.0.151 fork；待 ohmycloud 滚 catalog（G3 契约三点）与 lan-win 实跑回执 |
| D46 | Windows 构建切 gnu 交叉编译（用户裁 2026-09-14，摆脱 VC）：CI windows-latest msvc 岗撤、ubuntu 交叉岗（rustup target x86_64-pc-windows-gnu 加 apt mingw-w64）产 ark-x86_64-pc-windows-gnu.exe，PE 不可在 linux 跑测试面由 linux/mac 双岗覆盖；self update 资产读序三层（gnu 主名先、ark-x86_64-pc-windows-msvc.exe 回退名次（stable 段与历史 release 仅剩 msvc 资产的窗口期）、ome 兼容名殿后），官方与镜像链同构；旧 msvc 二进制对新 gnu 源无回退，升级走 omc catalog 通道重装；ohmycloud 侧已实证（WSL 交叉一次过含 ring 与 xz2、lan-win 实跑 --version 与 query 全过、CRT 静态零 DLL 依赖、尺寸同量级） | 已交付 | 第 0 轮（用户裁定 2026-09-14，改动清单到行级）；第 1 轮对线（codex F1/G1-G5 加 ohmycloud H1 双线回执，全修并入原批） | 本机交叉复验 PE32+ 12.4MB；cargo test --release 138 全绿；CI 四岗绿（run 34840170118），dev release 出 ark-x86_64-pc-windows-gnu.exe，桶 ark/dev 资产与边车双 200 |

## 维护规则

- 一条需求一行，D 编号两位接续（D01、D02…）。
- 新需求先入本表「待澄清」，经追问链澄清后流转「已澄清 / 已采纳」；状态生命周期：待澄清、已澄清、已采纳、已交付，任一状态可转「已拒绝」（记原因防复问）。
- 「派生去向」回指 GOAL 锚点 / PLAN 切片 / P 编号 / S 编号；目标交付后回填本表状态。
- 追溯条目必须标 [推断]；未标记者视为当期追问链产物。
